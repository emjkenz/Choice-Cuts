<div class="container">
    <div class="row">
        <div class="col-12">
            <h1>{{name}}</h1>
            <div>
                {{> partPicker}}
            </div>
            <p>Price: ${{price}}</p>
        </div>
    </div>
</div>
